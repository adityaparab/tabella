# Local overrides — the server also falls back to these same defaults, so copying
# this file to .env is only needed to change something (port 5433 matches compose.yaml).
DATABASE_URL=postgres://postgres:postgres@localhost:5433/tabella
